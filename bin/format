#!/bin/bash

find ./src -iname '*.hpp' -o -iname '*.cpp' | xargs clang-format -style=file -i
find ./lib -iname '*.hpp' -o -iname '*.cpp' | xargs clang-format -style=file -i
find ./tests -iname '*.hpp' -o -iname '*.cpp' | xargs clang-format -style=file -i

